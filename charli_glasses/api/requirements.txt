# CHARLI Glasses API Server dependencies
# Install: pip install -r requirements.txt

# Web framework + server
fastapi
uvicorn[standard]
python-multipart        # Required for file uploads (UploadFile)

# Speech-to-text (runs locally)
faster-whisper

# OpenAI-compatible API client (talks to OpenClaw on Mac Mini)
openai

# Audio processing
soundfile
numpy
